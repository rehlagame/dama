// js/config.js

const CONFIG = {
    ROWS: 8,
    COLS: 8,

    // تعريف هوية اللاعبين (أرقام لتسهيل الحسابات في المصفوفة)
    EMPTY: 0,
    PLAYER: 1,      // اللاعب البشري (يلعب من الأسفل للأعلى)
    ROBOT: -1,      // الروبوت (يلعب من الأعلى للأسفل)

    // تمييز بين الحجر العادي والشيخ
    PIECE_NORMAL: 1,
    PIECE_KING: 2,  // إذا كان الحجر شيخاً، سنضرب قيمته في هذه القيمة

    // حساب الاتجاهات الأساسية للداما الخليجية (أمام، يمين، يسار) - يمنع القطر
    DIRECTIONS: {
        UP: [-1, 0],
        DOWN: [1, 0],
        RIGHT: [0, 1],
        LEFT: [0, -1]
    }
};